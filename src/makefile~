# Makefile for building the C language shared library for the CalcMatrixAvg demonstration package.
C = gcc
OPTS = -c -fPIC
LOADER = gcc

OBJECTS = pearson.o

 CalcMatrixAvg.so: $(OBJECTS)
	R CMD SHLIB -o pearson.so $(OBJECTS)

.c.o: ; $(C) $(OPTS) -c $<

clean:
	-rm *.o *.so
